Place Fast Link-Up HLD diagrams here.

Expected filenames (export from Confluence as PNG or SVG):
- architecture-overview.png  — overall architecture of Fast Link-Up in SONiC
- global-config-flow.png     — flow for configuring global parameters and validations
- per-port-flow.png          — per-interface enable/disable orchestration flow
- link-recovery-sm.png       — state machine / decision flow for link recovery and fallback

Notes:
- Keep filenames and relative paths as-is; the HLD references these exact names.
- Prefer vector (SVG) when available; if using SVG, keep the same base names (e.g., architecture-overview.svg).

